{% extends 'base.html.twig' %}

{% block title %}New Image{% endblock %}

{% block body %}
    <body class="container bg-dark text-white mt-5">
        <h1 class="text-center mb-5">Ajouter des images à<br>{{ product.name }}</h1>

        {{ include('image/_form.html.twig') }}

        <a href="{{ path('image_index') }}">back to list</a>
    </body>
{% endblock %}
